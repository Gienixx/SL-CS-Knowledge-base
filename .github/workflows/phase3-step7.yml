name: Phase 3 Step 7 Archived View Guard

on:
  pull_request:
    paths:
      - 'agent-analytics.html'
      - 'scripts/report-details-agent-redirect.js'
      - 'scripts/reporting-source-cutover.js'
      - 'tests/phase3-step8-google-sheet-cutover.test.mjs'
      - '.github/workflows/phase3-step7.yml'
  push:
    branches: [main]
    paths:
      - 'agent-analytics.html'
      - 'scripts/report-details-agent-redirect.js'
      - 'scripts/reporting-source-cutover.js'
      - 'tests/phase3-step8-google-sheet-cutover.test.mjs'
      - '.github/workflows/phase3-step7.yml'

permissions:
  contents: read

jobs:
  cutover-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --check scripts/report-details-agent-redirect.js
      - run: node --check scripts/reporting-source-cutover.js
      - run: node --test tests/phase3-step8-google-sheet-cutover.test.mjs
